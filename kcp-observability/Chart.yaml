apiVersion: v2
name: kcp-observability
description: Prometheus alerts, recording rules, and a Grafana dashboard for a kcp.io control plane (shards, front-proxy, cache server, workspace/APIExport controllers) on Stakater Cloud.
type: application
version: 0.0.1
appVersion: "0.0.1"
