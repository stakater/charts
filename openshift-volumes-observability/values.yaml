# Default values for openshift-volumes-observability.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  namespace:

openshiftVolumesObservability:
  labels: